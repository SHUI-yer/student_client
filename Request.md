# 📋 学生信息管理系统 - 考核说明文档

这份考核的核心目的不是让你背诵 API 或配置，而是考察你在真实开发场景下的**综合研发能力**——在拿到一个前后端分离的项目需求时，能否独立查阅资料、跑通接口、编写出高可读性的代码，以及在遇到 Bug 时如何高效排查与解决。

---

## 🎯 一、 考核关注的六大核心能力

| 能力维度     | 具体考察点                                                   |
| :----------- | :----------------------------------------------------------- |
| **边做边学** | 遇到没见过的 API 或前端组件，能不能自己查阅官方文档独立搞定。 |
| **调试能力** | 页面报错了、接口不通了，你第一步看哪里（控制台、网络请求、后端日志）。 |
| **业务理解** | 功能做出来是不是用户真正需要的样子，交互是否符合正常逻辑。   |
| **接口联调** | 前后端同学配合时，请求参数、响应结构、HTTP 状态码你是否真的理解。 |
| **抽象复用** | 有没有意识到 *"这段代码我写过"*，然后想办法封装组件或函数，只写一次。 |
| **复盘反思** | 项目做完了，你从中学到了什么、踩了哪些坑（体现在踩坑日记与 README 中）。 |

---

## 💻 二、 项目核心需求排期

本项目共包含 **4 个核心页面**，总工期为三周，需在 **6 月 6 日** 前完成高质量交付。

### 1. 登录页 (Login Page)
* **核心机制**：后端使用 **JWT (JSON Web Token)** 进行安全认证。
* **开发重点**：
  * 实现标准的账号密码登录流程。
  * 登录成功后妥善管理后端返回的 `Token`（需考虑**页面刷新后 Token 的固化与恢复**）。
  * 后续所有业务请求（增删改查）都必须在请求头中携带该 Token 去访问。
  * **防呆拦截**：实现路由守卫，未登录的用户（无 Token）直接访问管理页时必须被拦截并重定向至登录页。

### 2. 学生信息管理页 (Student Management)
* **核心功能**：对学生信息的标准 **CRUD（增删改查）**。
* **界面要求**：
  * 使用大表格（`el-table`）展示学生列表。
  * 顶部提供按 **“姓名”**、**“学号”** 进行模糊搜索的能力。
  * 新增和编辑学生时，必须使用**模态弹窗表单（`el-dialog`）**，且编辑时需实现数据回显。
  * 删除学生时属于危险操作，必须弹出**二次确认提示框**。
* **🚀 可选高薪挑战**：表单里加一个头像上传功能，把图片文件通过 `FormData` 异步发送给后端。

### 3. 课程信息管理页 (Course Management)
* **核心功能**：对课程信息的标准 **CRUD（增删改查）**。
* **数据字段**：课程包含 `课程号`、`课程名`、`学分`、`授课教师`、`开课学期` 等核心字段。

### 4. 学生成绩查询页 (Score Inquiry)
* **核心功能**：多表关联查询与交叉报表展示。
* **界面要求**：
  * 该页面的数据不是来自单张表，而是后端将 **学生表、课程表、成绩表** 进行多表关联查询（`JOIN`）后组合返回的。
  * 前端需要将 `"学生姓名"、"课程名称"、"成绩"、"学分"` 这些来自不同表的字段在同一行表格中正确渲染。
  * 顶部支持按 **“学生”** 或按 **“课程”** 进行筛选，且两个条件必须支持**组合协同检索**。

---

## 🌐 三、 接口与工程层面的期望

### 1. 接口规范 (API Specification)
* **风格规范**：后端遵循 **RESTful** 风格，所有接口的响应格式统一为标准 `JSON`。
* **鉴权规范**：需要认证的接口，请求头里都必须携带：`Authorization: Bearer <你的Token>`。
* **敏捷变动**：具体的字段名、数据结构、分页参数格式，请在联调时和后端同学（或自己）对齐，不写死。需习惯接口文档随时发生微调的真实状况。

### 2. 代码组织与规整度
项目目录划分没有唯一正确答案，但必须体现你的架构思考过程：
* 请求相关的东西（Axios 实例、API 接口定义）是不是单独归类并放在一起了？
* 路由（Router）、状态管理（Store/Pinia）是不是各自有独立的目录？
* 页面组件（Views）和可复用的基础公共组件（Components）有没有清晰分开？
* 是否写了干净的注释？注释是否正确、清晰地反映了你的业务功能？
* 后端代码是否层级规整（Controller、Service、Mapper），能否第一时间精确定位到有 Bug 的功能在哪个类下。

### 3. UI 和交互体验
我们不追求炫酷，但追求**专业、像样**：
* 使用 **Element Plus** 的组件搭出一个合理的后台管理大布局（**左侧/顶部侧边栏菜单 + 右侧核心内容区**），拒绝把四个页面随便堆砌。
* **操作必须有即时反馈**：网络请求时必须有 `loading` 状态、成功时弹出绿色提示（Success）、失败时弹出红色警告（Error），不能让用户点了按钮后处于未知等待状态。
* 危险删除操作必须有确认步骤。整体视觉上配色统一、间距规律、层级清晰。

> ⚠️ **后端特别强调**：不要求用高大上的前沿算法或微服务写法来实现功能。**基础功能（CRUD、多表联查）的稳健实现与合理的数据库设计**才是本次考核的核心目的。

---

## 📦 四、 毕业交档与演示答辩要求

考核最终截止时间为三周，即 **6 月 6 号** 进行全盘验收。

### 1. 必须提交的成果文档
除工程源码外，项目根目录下必须提供以下文档：
* **`README.md`**：项目综合介绍、核心技术栈、本地一键启动/运行指南、主要核心功能运行截图。**这是别人了解你项目的第一扇窗户。**

### 2. 演示答辩流程 (5 - 15 分钟)
1. **功能全局走一遍**：从登录页开始，现场丝滑演示完 4 个页面的所有核心增删改查及多条件组合筛选。
2. **核心源码剖析**：挑一段最得意的代码现场讲解（例如：某个复杂功能从前端发起请求、Axios 拦截器加 Token、后端接收、MyBatis 查询数据库、再逐层返回的全流程，讲清前后端工具的使用）。
3. **踩坑复盘分享**：从你的踩坑记录里挑出一到两个印象最深、解决最巧妙的 Bug 痛点进行现场分享。

---

## 🏆 五、 评分等级说明

|     等级     | 判定标准                                                     |
| :----------: | :----------------------------------------------------------- |
|  🥇 **优秀**  | 四个页面功能完整且用户体验极度流畅；代码组织清晰、高复用、低耦合；文档详细且有技术深度；答辩时能完美讲清设计思路与工具链配合。 |
|  🥈 **良好**  | 核心功能全部跑通；代码结构基本合理；提供了基础的复盘踩坑文档。 |
|  🥉 **合格**  | 主流程能够磕磕绊绊走完；代码能跑，但代码组织和目录划分比较粗糙。 |
| ❌ **不合格** | 核心功能缺失；或者项目无法在本地成功启动运行。               |

---

## 💡 导师（Gemini）终极寄语
这份考核没有绝对的标准答案。遇到不会的技术点（例如 Vue3 的组合式 API、Axios 拦截器封装）非常正常。能查官方文档就查文档解决，解决不了就多讨论。**这不是闭卷考试，遇到 Bug、解决 Bug 并记录下来，本身就是拿“优秀”评级的核心过程。**

项目第一天的后端与数据库大捷已经为你拿到了高分入场券，让我们接下里的每一天都严格对照此表，稳扎稳打冲刺“优秀”！